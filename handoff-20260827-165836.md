# Handoff — 2026-08-27 16:58

## TL;DR
Mavis(MiniMax Code)接入 TencentDB-Agent-Memory Proxy 的工作做到一半。`custom_provider.memory-proxy` 条目已加到 `D:\minimax\.minimax\config.yaml`,baseURL 走 `/codebuddy/default/v1`(伪装成 codebuddy 接入,因 `/mavis/` 路由段在 proxy 端未启用)。curl 真实对话 200 OK,key 验证通过。**用户因扩展 SKILL 需要改动 SKILL.md + setup-proxy.sh 觉得"不好弄",临时换工作区。**

## Goal
让 Mavis 的 LLM 调用走 Memory Proxy,proxy 层把 session/memory/skill/knowledge 注入到 system prompt,行为记录落到 tidb 后端,用户能在 Panel (8125) 看到 Team / Agent / Task 维度的记忆。

## Non-goals
- 不动 `defaultModel`(用户明确说"不动",`custom_provider:jd/DeepSeek-V4-Flash` 保留为当前会话默认)
- 不动 proxy 服务端部署(没运维接入)
- 不做完整 L1/L2/L3 记忆沉淀验证(还没真正多次对话过)

## Decisions made
- **协议**:OpenAI Chat Completions(`/v1/chat/completions`)— 与 Mavis 自带 `custom_provider` 体系一致。
- **路由段**:用 `/codebuddy/default/v1` 伪装,而不是 `/mavis/default/v1`——因为 curl 探活发现 proxy 只启用了 `claude-code` 和 `codebuddy` 两个段(其他 6 个含 `/mavis/` 都返回 `missing service_id`)。SKILL.md 的 README §"其他平台接入"明确允许这种伪装。
- **model name**:`MiniMax-M3`(只放这一个,后续要 M2.7 等再说)。
- **provider id**:`memory-proxy`(顶层 key),**`name` 显示名改为 `tidb`**(用户手动改的,反映部署在 tidb 后端)。
- **手动 Edit 写 config 而非跑脚本**:`setup-proxy.sh` 第 91 行 `AGENTS=("claude-code" "codebuddy" "codex" "workbuddy" "dsh" "hermes" "openclaw")` 硬编码 7 个,不支持 Mavis;用户没让我扩展 SKILL+脚本,所以我用 Edit 工具直接 YAML 加条目(临时方案)。

## State of the work
- ✅ `D:\minimax\.minimax\config.yaml` 第 84-110 行新增 `custom_provider.memory-proxy` 条目
  - `name: tidb`
  - `api: openai-completions`
  - `baseURL: http://proxy.tidb.hkxhkx.site/codebuddy/default/v1`
  - `apiKey: sk-mem-***redacted***`(有效,curl 200 OK;前缀 `sk-mem-7Dv...`,后缀 `...Mrj-9P`)
  - `models.MiniMax-M3` 完整字段(attachment/reasoning/temperature/tool_call/limit/modalities)
- ✅ 原 config 备份在 `D:\minimax\.minimax\config.yaml.bak.mavis-proxy.20260827_162549`
- ✅ `defaultModel` 未动,`provider.minimax` / `custom_provider.deepseek` / `custom_provider.jd` 未动
- ✅ curl 验证:`POST /codebuddy/default/v1/chat/completions` 200 OK,响应 `model: "MiniMax-M3"`,`usage.prompt_tokens: 184`,`reasoning_tokens: 19` — 真实转发到 MiniMax 上游
- 🟡 **未做**:扩展 SKILL.md + setup-proxy.sh 加 mavis 为第 8 个 agent(用户暂停,说"不好弄")
- 🟡 **未做**:Step 10 资产导入(asset-import.ts)— 可选,跳过
- ⛔ **未做**:在 Panel (8125) 建 Team / Agent / Task 三元组 — INSTALL_CN.md §第 2 步要求,但 proxy 走 `/codebuddy/` 段后 session-init form 是否真弹 3 选 1 表单,需要一次真实 session 调用才能验证
- ⛔ **未做**:`defaultModel` 切换到 `custom_provider:memory-proxy/MiniMax-M3`(用户决定不动)

## Pointers to artifacts

- **改动文件**:`D:\minimax\.minimax\config.yaml` 第 84-110 行(`memory-proxy` 条目)
- **备份**:`D:\minimax\.minimax\config.yaml.bak.mavis-proxy.20260827_162549`
- **上游 SKILL**:`D:\git\workspace\260827接入tidb\agents\skills\setup-proxy\SKILL.md`
- **上游脚本**:`D:\git\workspace\260827接入tidb\agents\setup-proxy.sh`(第 91 行 AGENTS 数组 / 第 716-1065 行 write_xxx() 函数)
- **上游 INSTALL**:`D:\git\github\origin\TencentDB-Agent-Memory\INSTALL_CN.md`
- **Proxy 健康端点**:`http://proxy.tidb.hkxhkx.site/health` — 200,version 0.2.0
- **Proxy 路由可用情况**(本会话探活结果):
  - ✅ `/claude-code/default/...` 可用
  - ✅ `/codebuddy/default/...` 可用(本次使用)
  - ❌ `/opencode/` /dsh/ /codex/ /hermes/ /openclaw/ /workbuddy/ /mavis/ — 全部 `missing service_id`

## Open questions

1. **扩展 SKILL 路径**:把 Mavis 加为 SKILL 第 8 个 agent,需要:
   - SKILL.md:在 Step 3 选项表加 `8. Mavis (MiniMax Code)`、Step 5 加 mavis 的 curl 模板(Anthropic / OpenAI Chat / Responses 三协议)、Step 9.5 加 mavis 的切换说明(改 `defaultModel` 到 `custom_provider:memory-proxy/...`)
   - setup-proxy.sh:在 `AGENTS` 数组(91 行)加 `"mavis"`,新增 `write_mavis()` 函数(写 `D:\minimax\.minimax\config.yaml` 的 `custom_provider` 块),`DEFAULT_CONFIG_PATHS` 加对应路径,Step 8 case 分支加 `mavis) write_mavis "$CONFIG_PATH" ;;`
   - 然后**回滚当前 config 改动**,用 `bash setup-proxy.sh --non-interactive --proxy-host ... --user-key ... --agent mavis --model MiniMax-M3 --config-path D:\minimax\.minimax\config.yaml` 重做
   - 用户说"不好弄",要不要做?

2. **Panel 三元组**:要不要在 Panel (http://proxy.tidb.hkxhkx.site:8125,如果可达的话)建 Team / Agent / Task?目前没建过,proxy 第一次 session-init form 会空。

3. **`defaultModel` 切换**:用户说"不动",但当前会话仍是 `custom_provider:jd/DeepSeek-V4-Flash`,意味着即使 config.yaml 配了 memory-proxy,本会话和后续不主动切模型的会话都不会走 proxy。

4. **`x-task-id` 限制**:INSTALL §"已知限制"提到 Hermes/OpenClaw 必填 `x-task-id`,Mavis 伪装成 codebuddy 后这个限制是否适用?**未验证**。

5. **回滚命令**(如果新会话要恢复): `Copy-Item "D:\minimax\.minimax\config.yaml.bak.mavis-proxy.20260827_162549" "D:\minimax\.minimax\config.yaml" -Force`

## Suggested skills for next session
- `mavis` (memory / agent / skill sub-refs) — 如果接下来想用 cron 周期验证 proxy 健康度
- 无其它强相关 skill

## How to resume
"读 `D:\git\workspace\260827接入tidb\.minimax\handoff-20260827-165836.md`,决定要不要把 SKILL.md + setup-proxy.sh 扩展为支持 mavis(第 8 个 agent)。如果要,先回滚 `D:\minimax\.minimax\config.yaml` 到 `.bak.mavis-proxy.20260827_162549`,然后按 handoff 文档 §Open questions 第 1 项的指引改 SKILL + 脚本,再用 `--non-interactive` 模式重写。"